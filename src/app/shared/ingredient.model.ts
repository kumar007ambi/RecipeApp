export class Ingredient{
    // public name:string;
    // public amount:number;
    //adding public in constructor do the same as we do by like initiating before in commented
    constructor(public name:string,public amount:number){}
}